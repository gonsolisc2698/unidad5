const publicacion = "esri";
publicacion[0].toUpperCase() + publication.substring(1);
const publicacion = "environmental", 
publication[0].toUpperCase() + publication.substring(1);
const publicacion = "systems",
publication[0].toUpperCase() + publication.substring(1);
const publicacion = "research",
publication[0].toUpperCase() + publication.substring(1);
const publicacion = "systems",
publication[0].toUpperCase() + publication.substring(1);
const publicacion = "institute",
publication[0].toUpperCase() + publication.substring(1);
